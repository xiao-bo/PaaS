# Message Receiver
import os
from socket import *
import time
import datetime
import subprocess
import sys
import calendar
host = ""
buf = 1024
cmd=["cat /var/run/ptpd2.status.log|grep 'Offset'"]
cmd2=["ksh -c 'printf \"%(%s.%N)T\'"]
rece_time=subprocess.check_output(cmd2,shell=True) 
#send_time=data.split(':')[2]## let send time +offset
re_time=subprocess.check_output(cmd2,shell=True) 
#re_time=datetime.datetime.now()
epoch_time_milliseconds = int(time.time() * 1000)
#print "%.20f" % time.time()
dt=time.time()
print "%.20f" %dt
print dt
print rece_time
print re_time
